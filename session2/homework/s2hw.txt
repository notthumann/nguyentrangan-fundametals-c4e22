1. A Boolean value is either True or False. Three expressions are:
5 >= 6
"nott" + "humann" == "notthumann"
10 != 1
2. A flowchart is a type of diagram that represents an algorithm, workflow or process.
3. When there is another condition inside a condition, it is called nested condition.
	if name == "nott":
		if name == "humann":
			print("You are not human being.")